情緒儲存瓶 PWA

上傳方式（最簡單）：
1. 將本 ZIP 解壓縮。
2. 將資料夾內所有檔案上傳到任何靜態網站空間。
3. 用 iPhone Safari 開啟網站網址。
4. 點「分享」→「加入主畫面」。

注意：
- 必須透過 HTTPS 網址開啟，離線快取才會正常運作。
- 所有紀錄保存在使用者目前裝置的瀏覽器中。
- 不要只上傳 index.html；manifest、service-worker.js 與 icons 資料夾也要一起上傳。
